{% set name = "ibm-watson" %}
{% set version = "6.1.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: ea2cd3c7d84b10c0e8107da336a8e50a43b2ca01996ae1d0cbaad1e164ffdcf2

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - ibm-cloud-sdk-core >=3.3.6,<4
    - python >=3.6
    - python-dateutil >=2.5.3
    - requests >=2.0,<3.0
    - websocket-client ==1.1.0

test:
  imports:
    - ibm_watson
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/watson-developer-cloud/python-sdk
  summary: Client library to use the IBM Watson Services
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - synapticarbors
    - BastianZim
